import React from 'react';
import UnitModal from '../modal-unit/unit-modal.module';
const AddUnitModal = ({ addUnitModal }) => {
    return (<UnitModal unitModal={addUnitModal}/>);
}
 
export default AddUnitModal;